{
  "name": "bfa-vscode",
  "description": "VSCode extension for BFA",
  "author": "Sacha-Élie Ayoun",
  "license": "BSD-3-Clause",
  "version": "0.0.1",
  "publisher": "giltho",
  "repository": {
    "type": "git",
    "url": "https://github.com/bugs-for-all/bfa"
  },
  "type": "commonjs",
  "main": "./dist/bfa_vscode.bc.js",
  "files": [
    "dist",
    "LICENSE"
  ],
  "engines": {
    "vscode": "^1.75.0"
  },
  "activationEvents": [
    "onLanguage:c"
  ],
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "vscode": "^1.1.37",
    "@biomejs/biome": "^1.9.3",
    "@tsconfig/node22": "^22.0.0",
    "@tsconfig/strictest": "^2.0.5",
    "@types/vscode": "^1.75.1",
    "@vscode/test-electron": "^2.3.9",
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "watch": "esbuild _build/default/extension/bfa_vscode.bc.js --bundle --packages=bundle --external:vscode --outdir=dist --platform=node --target=es2022 --sourcemap --watch",
    "compile": "esbuild _build/default/extension/bfa_vscode.bc.js --bundle --packages=bundle --external:vscode --outdir=dist --platform=node --target=es2022 --sourcemap --minify",
    "package": "vsce package --out bfa-vscode.vsix --no-dependencies",
    "install": "code --force --install-extension bfa-vscode.vsix"
  }
}